<Window x:Class="NaraNote.App.Views.ReminderWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:loc="clr-namespace:NaraNote.App.Localization"
 Title="NaraNote" Width="420" SizeToContent="Height" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
 UseLayoutRounding="True" SnapsToDevicePixels="True" Icon="/NaraNote;component/Resources/NaraNote.ico"
 Background="#FFF2F2F7" FontFamily="Segoe UI" Foreground="#FF1C1C1E">
 <Window.Resources>
  <Style x:Key="CardStyle" TargetType="Border">
   <Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="#18000000"/><Setter Property="BorderThickness" Value="1"/>
   <Setter Property="CornerRadius" Value="10"/><Setter Property="Padding" Value="16"/>
  </Style>
  <Style TargetType="Button">
   <Setter Property="Height" Value="34"/><Setter Property="MinWidth" Value="78"/><Setter Property="Padding" Value="14,0"/>
   <Setter Property="Background" Value="#FFFFFFFF"/><Setter Property="Foreground" Value="#FF1C1C1E"/>
   <Setter Property="BorderBrush" Value="#20000000"/><Setter Property="BorderThickness" Value="1"/><Setter Property="FontWeight" Value="SemiBold"/>
   <Setter Property="FocusVisualStyle" Value="{x:Null}"/><Setter Property="Cursor" Value="Hand"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
    <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
     <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Border>
    <ControlTemplate.Triggers>
     <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#FFF5F5F7"/></Trigger>
     <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Opacity" Value="0.72"/></Trigger>
     <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4"/></Trigger>
    </ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
   <Setter Property="Background" Value="#FF0A84FF"/><Setter Property="Foreground" Value="White"/><Setter Property="BorderBrush" Value="#FF0A84FF"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
    <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
     <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Border>
    <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#FF0077E6"/></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Opacity" Value="0.72"/></Trigger></ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style x:Key="DestructiveButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
   <Setter Property="Foreground" Value="#FFFF3B30"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderBrush" Value="Transparent"/><Setter Property="FontWeight" Value="Normal"/>
  </Style>
  <Style TargetType="ComboBox">
   <Setter Property="Height" Value="34"/><Setter Property="Background" Value="#FFF7F7F9"/><Setter Property="BorderBrush" Value="#22000000"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="9,4"/>
   <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ComboBox"><Grid>
    <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7"/>
    <ContentPresenter x:Name="SelectionPresenter" Margin="10,3,30,3" VerticalAlignment="Center" Content="{TemplateBinding SelectionBoxItem}" IsHitTestVisible="False"/>
    <TextBox x:Name="PART_EditableTextBox" Margin="4,3,29,3" Padding="6,0" VerticalContentAlignment="Center" Background="Transparent" BorderThickness="0" Visibility="Collapsed" Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    <ToggleButton Width="30" HorizontalAlignment="Right" Focusable="False" Background="Transparent" BorderThickness="0" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
     <ToggleButton.Template><ControlTemplate TargetType="ToggleButton"><Border Background="Transparent" CornerRadius="0,7,7,0"><Path Width="8" Height="5" Fill="#FF636366" Stretch="Fill" Data="M 0 0 L 8 0 L 4 5 Z"/></Border></ControlTemplate></ToggleButton.Template>
    </ToggleButton>
    <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True" IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Fade">
     <Border Margin="0,4,0,0" Padding="3" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}" Background="White" BorderBrush="#24000000" BorderThickness="1" CornerRadius="7"><ScrollViewer><ItemsPresenter/></ScrollViewer></Border>
    </Popup>
   </Grid><ControlTemplate.Triggers>
    <Trigger Property="IsEditable" Value="True"><Setter TargetName="SelectionPresenter" Property="Visibility" Value="Collapsed"/><Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/></Trigger>
    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="BorderBrush" Value="#660A84FF"/></Trigger>
    <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="Surface" Property="BorderBrush" Value="#FF0A84FF"/><Setter TargetName="Surface" Property="BorderThickness" Value="2"/></Trigger>
   </ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style TargetType="CheckBox">
   <Setter Property="FocusVisualStyle" Value="{x:Null}"/><Setter Property="Cursor" Value="Hand"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="CheckBox"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="24"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <Border x:Name="Box" Width="18" Height="18" Background="White" BorderBrush="#FF8E8E93" BorderThickness="1" CornerRadius="5" VerticalAlignment="Center">
     <Path x:Name="Mark" Width="10" Height="7" Data="M 1,3.5 L 4,6 L 9,1" Stroke="White" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed"/>
    </Border>
    <ContentPresenter Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center"/>
   </Grid><ControlTemplate.Triggers>
    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Box" Property="BorderBrush" Value="#FF0A84FF"/></Trigger>
    <Trigger Property="IsChecked" Value="True"><Setter TargetName="Box" Property="Background" Value="#FF0A84FF"/><Setter TargetName="Box" Property="BorderBrush" Value="#FF0A84FF"/><Setter TargetName="Mark" Property="Visibility" Value="Visible"/></Trigger>
    <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Box" Property="BorderThickness" Value="2"/><Setter TargetName="Box" Property="BorderBrush" Value="#FF0A84FF"/></Trigger>
   </ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
  </Style>
 </Window.Resources>
 <Grid Margin="22,18,22,20">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <StackPanel Grid.Row="0" Margin="2,0,0,14">
   <TextBlock Text="{loc:Loc Reminder}" FontSize="22" FontWeight="SemiBold"/>
   <TextBlock Text="{loc:Loc ReminderDescription}" Margin="0,4,0,0" Foreground="#FF6E6E73" FontSize="12.5"/>
  </StackPanel>

  <Border Grid.Row="1" Style="{StaticResource CardStyle}">
   <StackPanel>
    <TextBlock Text="{loc:Loc Date}" FontSize="12" FontWeight="SemiBold" Foreground="#FF6E6E73" Margin="0,0,0,8"/>
    <Calendar x:Name="ReminderCalendar" HorizontalAlignment="Center" DisplayMode="Month" SelectionMode="SingleDate" FontSize="13" BorderThickness="0" Background="Transparent">
     <Calendar.LayoutTransform><ScaleTransform ScaleX="1.34" ScaleY="1.34"/></Calendar.LayoutTransform>
    </Calendar>
   </StackPanel>
  </Border>

  <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,10,0,0">
   <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <DockPanel Grid.Row="0" Margin="0,0,0,11">
     <TextBlock Text="{loc:Loc Time}" FontSize="12" FontWeight="SemiBold" Foreground="#FF6E6E73" VerticalAlignment="Center"/>
     <CheckBox x:Name="Use24HourCheck" Content="{loc:Loc Use24Hour}" DockPanel.Dock="Right" Height="24" HorizontalAlignment="Right" Checked="TimeFormatCheck_Changed" Unchecked="TimeFormatCheck_Changed"/>
    </DockPanel>
    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
     <ComboBox x:Name="AmPmBox" Width="74" Margin="0,0,8,0" PreviewMouseLeftButtonDown="DropDownComboBox_PreviewMouseLeftButtonDown"><ComboBoxItem Content="{loc:Loc AM}"/><ComboBoxItem Content="{loc:Loc PM}"/></ComboBox>
     <ComboBox x:Name="HourBox" Width="72" IsEditable="True" IsTextSearchEnabled="False" MaxDropDownHeight="240" ToolTip="{loc:Loc Hour}" DropDownOpened="TimePart_DropDownOpened"/>
     <TextBlock Text=":" FontSize="20" FontWeight="SemiBold" Margin="8,0" VerticalAlignment="Center" Foreground="#FF636366"/>
     <ComboBox x:Name="MinuteBox" Width="72" IsEditable="True" IsTextSearchEnabled="False" MaxDropDownHeight="240" ToolTip="{loc:Loc Minute059}" DropDownOpened="TimePart_DropDownOpened"/>
    </StackPanel>
   </Grid>
  </Border>

  <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,10,0,0">
   <StackPanel>
    <Grid>
     <Grid.ColumnDefinitions><ColumnDefinition Width="70"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <TextBlock Text="{loc:Loc Repeat}" FontWeight="SemiBold" VerticalAlignment="Center"/>
     <ComboBox x:Name="RecurrenceBox" Grid.Column="1" SelectionChanged="RecurrenceBox_SelectionChanged" PreviewMouseLeftButtonDown="DropDownComboBox_PreviewMouseLeftButtonDown">
      <ComboBoxItem Content="{loc:Loc RepeatOnce}"/><ComboBoxItem Content="{loc:Loc RepeatDaily}"/><ComboBoxItem Content="{loc:Loc RepeatWeekly}"/><ComboBoxItem Content="{loc:Loc RepeatWeekdays}"/>
     </ComboBox>
    </Grid>
    <StackPanel x:Name="WeekdayPanel" Visibility="Collapsed">
     <Separator Margin="0,13,0,12" Background="#16000000"/>
     <UniformGrid Columns="7">
      <CheckBox x:Name="Sunday" Content="{loc:Loc Sun}"/><CheckBox x:Name="Monday" Content="{loc:Loc Mon}"/><CheckBox x:Name="Tuesday" Content="{loc:Loc Tue}"/><CheckBox x:Name="Wednesday" Content="{loc:Loc Wed}"/><CheckBox x:Name="Thursday" Content="{loc:Loc Thu}"/><CheckBox x:Name="Friday" Content="{loc:Loc Fri}"/><CheckBox x:Name="Saturday" Content="{loc:Loc Sat}"/>
     </UniformGrid>
    </StackPanel>
   </StackPanel>
  </Border>

  <Border Grid.Row="4" Style="{StaticResource CardStyle}" Margin="0,10,0,0">
   <StackPanel>
    <CheckBox x:Name="AutoHideCheck" Content="{loc:Loc AutoHide}" Height="24"/>
    <TextBlock Text="{loc:Loc AutoHideDescription}" Margin="28,3,0,0" FontSize="11.5" Foreground="#FF8E8E93"/>
   </StackPanel>
  </Border>

  <Grid Grid.Row="5" Margin="0,18,0,0">
   <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <Button x:Name="DisableButton" Content="{loc:Loc ReminderDisable}" Style="{StaticResource DestructiveButtonStyle}" HorizontalAlignment="Left" Click="Disable_Click"/>
   <Button Grid.Column="1" Content="{loc:Loc Cancel}" Margin="0,0,8,0" IsCancel="True"/>
   <Button Grid.Column="2" Content="{loc:Loc Ok}" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" Click="Ok_Click"/>
  </Grid>
 </Grid>
</Window>
